# Neon PostgreSQL connection string (SSL required)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Better Auth JWT Secret (generate a secure random string, min 32 characters)
BETTER_AUTH_SECRET=your-secret-key-here-min-32-characters-long

# JWT Configuration
JWT_ALGORITHM=HS256
JWT_EXPIRE_DAYS=7
